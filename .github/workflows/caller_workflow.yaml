name: Caller Workflow Test

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: "Ambiente de build"
        required: true
        type: choice
        options:
          - producaoCloudPrd
          - pilotoCloudPrd

  push:
    branches:
      - main

jobs:
  test:
    uses: brenhe/flow-test/.github/workflows/workflow_call.yaml@main
    with:
      build_type: ${{ inputs.build_type }}
  test2:
    runs-on: ubuntu-latest
    steps:
      - name: Run input value test
        run: |
            echo "Running tests for build type: ${{ inputs.build_type }}"
        shell: bash