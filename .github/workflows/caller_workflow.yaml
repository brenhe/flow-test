name: Caller Workflow Test

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: "Ambiente de build"
        required: true
        type: choice
        options:
          - producaoCloudPrd
          - pilotoCloudPrd

jobs:
  Chamando_Workflow_Call:
    uses: brenhe/flow-test/.github/workflows/workflow_call.yaml@main
    with:
      build_type: ${{ inputs.build_type }}